(() => { const state={page:1,limit:12}; const $=id=>document.getElementById(id); const status=$('status'), list=$('articles'), more=$('load-more');
function option(select,value){const o=document.createElement('option');o.value=value;o.textContent=value;select.appendChild(o)}
async function load(reset=true){if(reset){state.page=1;list.replaceChildren()} more.disabled=true; status.textContent='Loading articles…'; const p=new URLSearchParams({page:state.page,limit:state.limit,sort:$('sort').value}); const q=$('search').value.trim(); if(q)p.set('q',q); if($('category').value)p.set('category',$('category').value); if($('source').value)p.set('source',$('source').value); try{const r=await fetch('/api/articles?'+p);if(!r.ok)throw Error('API request failed');const d=await r.json(); if(reset&&!d.total){const e=document.createElement('div');e.className='empty';const h=document.createElement('h2');h.textContent='No published articles yet';const x=document.createElement('p');x.textContent='Run the AI Scout pipeline to publish the first article.';e.append(h,x);list.append(e)}else if(!d.items.length){status.textContent='No results';}else{d.items.forEach(render);status.textContent=`${d.total} published articles`;more.hidden=state.page*state.limit>=d.total} }catch(e){status.textContent='Unable to load articles. Please try again.'}finally{more.disabled=false}}
function render(x){const card=document.createElement('article');card.className='card';const h=document.createElement('h2');h.textContent=x.title||'Untitled';const p=document.createElement('p');p.textContent=x.summary||'';const m=document.createElement('div');m.className='meta';m.textContent=`${x.source||''} · ${x.category||''} · ${x.published_at||''}`;const s=document.createElement('div');s.className='score';s.textContent=`AI Scout score ${Math.round((x.score||0)*100)}/100`;const a=document.createElement('a');a.href='/article/'+encodeURIComponent(x.id);a.textContent='Read article';const o=document.createElement('a');o.href=x.url||'#';o.target='_blank';o.rel='noopener';o.textContent='Original source';card.append(h,p,m,s,a,o);list.append(card)}
['search','category','source','sort'].forEach(id=>$(id).addEventListener(id==='search'?'input':'change',()=>load())); more.addEventListener('click',()=>{state.page++;load(false)}); fetch('/api/categories').then(r=>r.json()).then(d=>Object.keys(d).forEach(x=>option($('category'),x))).catch(()=>{}); fetch('/api/sources').then(r=>r.json()).then(d=>Object.keys(d).forEach(x=>option($('source'),x))).catch(()=>{}); load(); })();
